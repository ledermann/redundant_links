puts "
Plugin redundant_links installed, now generate the migration with this command:
   ./script/generate redundant_link_migration add_redundant_links_table

After this, migrate your database:
   rake db:migrate

"